import UIKit

extension R {
    enum ImagesYP {
        enum TabBar {
            static let tracker = UIImage(resource: .trackerYP)
            static let statistic = UIImage(resource: .statisticYP)
        }
    }
}
